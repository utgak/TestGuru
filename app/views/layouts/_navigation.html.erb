<% if logged_in? %>
  <p class="nav user">
    Welcome, <b><%= current_user.email %></b> Guru
    <%= link_to 'Test Guru', root_path %>
    <%= link_to 'Exit', sessions_path, method: :delete %>
  </p>
<% else %>
  <p class="nav user">
    <%= link_to 'Signup', signup_path %> |
    <%= link_to 'Login', login_path %>
  </p>
<% end %>