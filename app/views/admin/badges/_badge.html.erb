<tr>
  <td><%= badge.title %></td>
  <td><%= image_tag(badge.img) %></td>
  <td><%= badge.condition %></td>
  <td><%= badge.option %></td>
  <td><%= link_to t('show'), admin_badge_path(badge) %></td>
  <td><%= link_to t('edit'), edit_admin_badge_path(badge) %></td>
  <td><%= link_to t('delete'), admin_badge_path(badge), method: :delete, data: { confirm: t('are_you_sure?') } %></td>
</tr>