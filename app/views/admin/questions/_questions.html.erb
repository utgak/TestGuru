<tr>
  <td><%= questions.title %></td>
  <td><%= link_to t('edit'), edit_admin_question_path(questions) %></td>
  <td><%= link_to t('show'), admin_question_path(questions) %></td>
  <td><%= link_to t('delete'), admin_question_path(questions),
                  method: :delete,
                  data: { confirm: t('are_you_sure?') }%></td>
</tr>
