<tr>
  <td><%= questions.title %></td>
  <td><%= link_to 'Edit', edit_admin_question_path(questions) %></td>
  <td><%= link_to 'Show', admin_question_path(questions) %></td>
  <td><%= link_to 'Delete', admin_question_path(questions),
                  method: :delete,
                  data: { confirm: 'Are you sure?'}%></td>
</tr>
